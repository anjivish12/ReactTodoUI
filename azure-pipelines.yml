trigger:
- develop
- features

pool: anjalipool

stages:

# - 

- stage: deployvm
  displayName: deploy VM
  jobs:
  - job: deploye
    displayName: deploye vm
    steps:
    - task: DownloadPipelineArtifact@2
      inputs:
        buildType: 'specific'
        project: '587fbf2b-269b-4579-8924-db577d7138dc'
        definition: '40'
        buildVersionToDownload: 'specific'
        pipelineId: '612'
        artifactName: 'build'
        targetPath: '$(System.DefaultWorkingDirectory)/anjali_artifatcs'

    